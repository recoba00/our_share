# CHANGELOG

Codex가 작업한 내용을 날짜별로 누적 기록한다.

## 2026-09-24

- 설정에 전체 화면 신고 접수 폼을 추가하고 신고 대상·사유·상세 내용을 운영자 전용 대기열에 저장하도록 구현했다.
- 백오피스에 `신고 관리` 메뉴와 처리 대기·전체 기록 페이지네이션, 처리 완료·문제 없음 기록 기능을 추가했다.
- 사용자 목록에 이용 상태와 제한 관리 기능을 추가하고 제한 상태를 Firestore·Realtime Database에 함께 반영하도록 구현했다.
- 제한된 계정은 전용 안내 화면만 볼 수 있으며 Firestore 데이터와 실시간 위치·접속 상태 접근이 보안 규칙에서 차단되도록 강화했다.
- 사용자 증가를 고려해 백오피스의 전체 사용자·크루·멤버십 실시간 구독을 제거하고 Firestore 서버 집계 쿼리와 최근 5개 제한 조회로 전환했다.
- 사용자·크루 목록을 25개 단위 커서 페이지네이션으로 바꾸고, 이름 앞부분 검색·이전/다음 이동·수동 새로고침·독립 오류 상태를 추가했다.
- 공개 프로필과 크루명 검색, 관리자 집계 쿼리, 일반 사용자의 전체 크루·멤버십 집계 차단을 Firestore Rules 에뮬레이터에서 검증했다.
- 검증: `npm run lint`, `npm run test:unit`(12개), `npm run test:rules`(48개), `npm run build:firebase`, `npm run test:dist:firebase` 통과.

## 2026-09-23

- 운영자 도구를 일반적인 백오피스 형태로 개편해 데스크톱 사이드바, 모바일 탭, 운영 지표, 최근 공지, 사용자 공개 프로필 목록, 크루·멤버 현황과 검색을 한 화면 체계로 통합했다.
- 플랫폼 관리자만 전체 크루와 멤버십을 읽을 수 있도록 Firestore 규칙을 좁게 확장하고, 비관리자의 전체 목록 조회와 관리자의 비공개 사용자 문서 조회가 계속 차단되는 회귀 테스트를 추가했다.
- 관리자 전용 `/admin` 운영자 도구를 추가해 공지를 작성·수정·게시·삭제하고 설정의 공지사항 화면에서 게시 공지를 실시간으로 확인할 수 있게 했다.
- Firestore `serviceNotices`에 플랫폼 관리자 전용 쓰기, 로그인 사용자의 게시 공지 전용 읽기, 필드·길이·불변값 검증 규칙을 추가했다.
- 이메일과 약관 동의 기록이 있는 `users` 문서를 본인 전용으로 강화하고, 크루 멤버 표시에 필요한 이름·프로필 이미지는 `publicProfiles`로 분리했다.
- 기존 계정의 공개 프로필을 Hosting 배포 전에 Admin SDK로 자동 보정하도록 배포 workflow를 확장했다.
- 공지 권한 상승, 초안 노출, 스키마 오염, 과대 본문, 사용자 개인정보 노출을 막는 Firestore Rules 회귀 테스트를 추가했다.
- Firebase 인증 상태를 복원하는 동안 비회원 홈이 잠깐 노출되지 않도록 전용 로그인 확인 화면을 추가했다.
- Realtime Database 권한 코드 표기를 정규화해 `PERMISSION_DENIED` 같은 원문 대신 짧고 이해하기 쉬운 안내가 표시되도록 보강했다.
- Firestore 멤버십을 기준으로 Realtime Database 권한 미러를 보정하는 Spark 요금제용 배포 스크립트를 추가하고, Hosting 자동 배포 전에 실행하도록 연결했다.
- 누락됐던 크루장 멤버십 미러를 자동 복구한 뒤 실서비스에서 위치 구독 권한 오류가 재발하지 않는 것을 확인했다.
- 비회원 시작 화면의 배경·로고·문구·로그인 폼 전환을 2.9초 안에서 더 느리고 부드럽게 조정했다.
- 검증: `npm run lint`, `npm run test:unit`(11개), `npm run test:rules`(37개), Functions 테스트, Firebase Hosting 빌드·산출물 검사·실배포 검증 통과.

## 2026-09-22

- 비로그인 사용자는 홈만 볼 수 있도록 보호하고, 초대 링크 코드는 보존한 채 로그인 후 참여 흐름을 이어가게 했다. 게스트 홈은 제공 이미지 배경을 화면 전체에 맞추고 50% 어두운 오버레이, 서비스 소개, Google 가입·로그인 버튼으로 구성했으며 헤더·하단 탭과 PWA 설치/업데이트 배너를 숨긴다.
- 실서비스 QA에서 현재 크루의 RTDB 멤버 권한 미러 중 크루장 레코드 누락을 확인해 복구했다. 새로고침 후 위치 구독 권한 오류가 더 이상 표시되지 않는 것을 확인했다.
- 검증: `npm run lint`, `npm run test:unit`(8개), `npm run build:firebase`, `npm run test:dist:firebase` 통과.
- 실서비스 QA에서 채팅 메시지를 삭제해도 채팅 목록의 최근 메시지 미리보기가 남는 문제를 확인했다. 메시지 삭제 후 최신 잔여 메시지로 미리보기를 재계산하고, 동시에 새 메시지가 도착하면 오래된 값으로 덮지 않도록 트랜잭션에서 조건부 갱신한다.
- 채팅 미리보기의 최신 메시지 선택, 마지막 메시지 삭제, 원본 목록 비변경 테스트를 추가했다.
- 진단 저장 권한 검사가 실제 위치 경로에 샘플 좌표를 쓰고 삭제해 공유 위치를 덮어쓸 수 있던 문제를 막았다. 위치 쓰기 검사는 건너뛰고 안내하며, 진단용 일정·메모·투표·비밀방은 오류가 나도 정리 작업을 시도한다.
- 홈과 채팅에서 멤버 조회 중 빈 배열을 실제 멤버 없음으로 오인해 보여주던 순간 상태를 로딩·오류·완료 상태로 구분했다.
- 투표 삭제 시 고아 투표 기록이 남지 않도록 관련 표 문서를 먼저 정리하고, 본인 소유 투표의 멤버 기록만 삭제할 수 있게 Firestore 규칙과 테스트를 보강했다.
- 검증: `npm run lint`, `npm run test:unit`(8개), `npm run test:rules`(37개), `npm run build:firebase`, `npm run test:dist:firebase` 통과.
- 실서비스 QA에서 첫 1:1 채팅 진입 시 모바일 헤더가 일반 제목으로 남는 현상을 확인하고, 방·멤버 데이터 로딩 후 실제 채팅방 이름을 라우트 상태에 동기화하도록 수정했다.
- 검증: `npm run lint`, `npm run test:unit`(5개), `npm run build:firebase`, `npm run test:dist:firebase` 통과.
- 입력 필드는 한국어 텍스트 키보드를 기본으로 사용하고, 초대코드는 영문 대문자·숫자 6자리 입력, 이미지 URL은 URL 키보드, 민감 메모 비밀번호는 숫자 키패드를 우선 표시하도록 용도별 입력 속성을 지정했다. 채팅 검색·메시지와 설명·투표 항목에도 목적에 맞는 키보드 힌트를 적용했다.
- 크루장 승계 시 초대 인덱스 소유자 정보도 같은 Firestore 배치에서 갱신하고, 이전 크루장 권한으로 크루 삭제가 막히던 Rules 검증을 보강했다.
- 크루 멤버 삭제 시 Realtime Database 위치·멤버 미러를 정리하고, 크루 목록 로딩 중 멤버십 미러 보정이 끝나기 전에 위치 공유를 시도하던 권한 오류를 수정했다.
- 위치 공유 권한 거절 메시지를 알아보기 쉬운 안내로 바꾸고 로그아웃 완료 토스트를 추가했다.
- Hosting 배포 workflow에 Firestore·Realtime Database Rules 배포를 연결하고 수동 재실행 기능을 추가했다. `35674454249`에서 Rules와 Firebase Hosting 배포 및 배포 검증이 모두 성공했다.
- 검증: `npm run lint`, `npm run test:rules`(33개), `npm run test:unit`(5개), `npm run build:firebase`, `npm run test:dist:firebase` 통과.
- 채팅 상세 참여 멤버가 크루장에게 모어 버튼을 보여주고, 기존 바텀시트에서 역할 변경·크루장 승계·채팅방 내보내기·크루에서 내보내기를 방 유형에 맞게 선택할 수 있도록 확장했다.
- 데스크탑 헤더의 로그아웃 버튼을 제거하고 내 정보 화면에서만 로그아웃하도록 정리했다.
- 채팅 참여 멤버 아이콘을 Phosphor `Users` 아이콘으로 교체했다.

## 2026-09-21

- 오래된 PWA JS 청크로 발생하는 `Importing a module script failed` 오류를 감지하면 한 번만 최신 배포본으로 새로고침하도록 복구 처리를 추가했다.
- 위치 데이터가 Realtime Database에서 갱신될 때 카카오 지도 중심을 재설정하지 않고 최신 좌표로 핀만 즉시 다시 투영하도록 개선했다.
- 크루를 전환하거나 위치 공유를 다시 시작할 때 이전 크루의 전송 기준 좌표가 섞이지 않도록 위치 공유 상태를 초기화했다.
- 홈 크루 상황 멤버 행을 `프로필 → 이름 → 역할 뱃지` 순서로 정렬하고 중복되던 오너 행의 `총 N명` 표기를 제거했다.
- 제공받은 크루 이미지로 OG 공유 이미지를 교체하고 1200×630px 비율로 최적화했다.
- 앱 이름을 `우리끼리`로 통일하고 홈 헤더의 브랜드 아이콘을 PWA 설치 아이콘으로 재사용했다.
- Firebase Hosting 루트와 `/our_share/` 하위 경로 모두에서 manifest·favicon·Apple touch icon을 찾도록 상대 경로로 변경했다.
- Hosting push 배포가 Firebase Rules IAM 오류로 중단되지 않도록 메인 workflow에서 Rules 단계를 제거하고, Firestore/Realtime Database Rules는 각 수동 workflow에서 독립적으로 배포하도록 정리했다.
- `og:image`와 Twitter 카드 메타 태그, 1200×630 공유 이미지를 추가해 링크 공유 미리보기를 정리했다.
- 키보드 입력이 필요한 채팅 투표 생성·메시지 수정·크루 생성 UI를 공통 전체 화면 입력 레이어로 통일했다.
- 전체 화면 입력 레이어가 모바일 `visualViewport` 크기를 따라가도록 보완해 키보드가 열려도 포커스 필드와 닫기 헤더가 가려지지 않게 했다.
- Firestore·Realtime Database Rules 배포 권한 부족으로 Hosting 자동 배포가 막히지 않도록 Hosting workflow와 Rules 배포를 분리했다. Rules는 별도 수동 배포 대상으로 유지한다.

- 무료 Spark 운영 정책을 문서화하고 Firebase Storage·Cloud Functions 운영 배포를 MVP 범위에서 제외했다.
- Functions 수동 배포 workflow에 `confirm_blaze_deploy` 확인 입력을 추가해 Blaze 전환 없이는 운영 배포가 실행되지 않도록 보호했다.
- 채팅 상세 헤더에 참여 멤버 메뉴를 추가하고, 일반 크루방·1:1·비밀방별 참여자 목록을 구분해 보여주도록 했다.
- 참여 멤버 드로어에서 프로필·역할 아이콘·멤버 관리를 확인할 수 있게 하고, 크루장에게만 역할 변경·크루장 승계·멤버 내보내기를 노출했다.
- 채팅 멤버 내보내기를 크루 삭제와 분리해 비밀방의 참여자 목록에서만 제거하도록 변경했다. 홈 크루 상황에서는 별도로 `크루에서 내보내기`를 제공한다.
- 채팅 상세 멤버 진입 아이콘을 햄버거에서 유저 목록 아이콘으로 바꾸고 알림 아이콘 왼쪽에 배치했다.
- 채팅 멤버 제거 Rules를 실서비스에 반영할 수 있도록 무료 운영 환경용 수동 Firestore Rules 배포 workflow를 추가했다.
- Hosting push 배포 workflow에 Firestore·Realtime Database Rules 배포를 포함해 코드와 Rules가 함께 실서비스에 반영되도록 했다.
- 홈 크루 현황과 채팅 참여 멤버 행을 공통 `CrewMemberRow`로 통합하고, 프로필·이름·역할·액션 순서와 옅은 카드 구분을 일원화했다.

## 2026-09-17

- 역할 변경·크루장 승계가 Firestore 저장 후 RTDB 미러 권한 오류로 실패처럼 보이지 않도록 미러 갱신을 best-effort로 처리했다.
- 크루 역할에 `부크루장`을 추가하고 크루장·부크루장·멤버 표기를 공통 왕관 아이콘과 역할명으로 연결했다. 크루장은 골드, 부크루장은 브론즈 색상으로 구분한다.
- 크루장이 멤버 관리에서 역할을 `멤버` 또는 `부크루장`으로 변경할 수 있게 하고, 부크루장은 크루당 최대 2명으로 제한했다.
- 크루장 승계 전용 확인 다이얼로그를 추가하고, 승계 시 기존 크루장은 부크루장으로 내려가며 새 멤버가 크루장이 되도록 Firestore 배치로 원자 처리했다.
- 부크루장의 초대 기능은 허용하되 크루 삭제·멤버 관리 권한은 크루장에게만 유지하고, 부크루장 탈퇴·삭제 시 역할 슬롯을 정리한다.
- Firestore·Realtime Database 역할 규칙과 Firebase Functions 멤버십 미러를 `VICE_OWNER`까지 확장하고 승계·최대 2명·크루 삭제 권한 테스트를 추가했다.
- Hosting 자동 배포 workflow에서도 Firestore 규칙을 함께 배포하도록 서비스 계정 임시 인증 단계를 추가했다.
- `VICE_OWNER` RTDB 미러 저장이 운영 규칙에서 막히지 않도록 Hosting 자동 배포에 Realtime Database 규칙 배포도 포함했다.

## 2026-09-16

- `BottomSheet`, `Modal`, 전체 화면 입력 레이어, 확인 다이얼로그를 공통 `DialogSurface`로 묶어 64px 헤더·아이콘 닫기·16px 본문 패딩·버튼 영역을 통일했다.
- 설정 화면에 공지사항, 서비스 이용약관, 개인정보 처리방침, 위치정보 이용 안내, 앱 버전·배포 커밋 정보를 추가했다.
- Google 로그인 전 서비스 이용약관과 개인정보 처리방침 필수 동의 흐름을 공통 바텀시트로 연결했다.
- 기존 로그인 사용자가 최신 정책에 동의하지 않은 경우 서비스 진입 전에 동의하도록 게이트를 추가했다.
- 위치 공유 시작 전에 공유 범위, 갱신 주기, 권한 거절 시 영향과 백그라운드 한계를 안내하는 바텀시트를 추가했다.
- 위치정보 이용약관에 실제 수집 항목·제공 대상·보유 기간·국외 처리 내용을 보강하고 위치 공유 시작 전 별도 동의 체크를 추가했다.
- 개인정보 처리방침·위치정보 이용약관·공지사항 다이얼로그의 문서 영역을 내부 스크롤로 분리하고 상하 페이드 마스크, 스크롤바 숨김, 터치 스크롤 관성을 적용했다.
- 정책 동의 기록은 브라우저에 버전과 시각을 저장하고, 로그인 사용자 프로필에도 동기화하도록 보강했다.
- MVP 정책 문서에 운영자 정보와 법률 검토가 필요한 항목을 명시했다.
- 토스트의 배경·보더를 테마 토큰에 연결하고 Firebase 권한·연결 오류가 기술 문구 대신 짧은 사용자 안내로 보이도록 정리했다.
- 생성·수정·삭제·참여·로딩·빈 상태 메시지를 `~했어요`·`~없어요` 말투로 통일하고, `크루방`을 `크루 채팅방`으로 명확하게 다듬었다.
- 확인·삭제 다이얼로그의 경고 문구와 초대·위치·지도·PWA 안내까지 짧고 친근한 UX 라이팅으로 정리했다.
- 내 정보와 크루 전환 탭을 `내 크루`·`참여 크루`로 바꾸고, 크루 참여·멤버 관리·채팅방 생성 관련 안내 문구를 자연스럽게 다듬었다.
- 홈 크루 정보 영역의 중첩 카드 보더·패딩을 제거하고, 크루장 표기를 작은 오렌지색 필 왕관 아이콘으로 변경해 크루명 앞에 배치했다.
- 크루명 입력 제한과 기존 이름 말줄임 규칙을 단위 테스트로 고정하고 MVP 통합 테스트에 포함했다.
- 크루장 역할 표기를 텍스트 뱃지 대신 공통 왕관 아이콘으로 통일하고, 메인 위치 섹션 명칭을 `멤버 위치`로 변경했다.
- 크루 이름 입력을 표시 규칙과 동일한 8자 이내로 제한하고 생성·수정 서비스에서도 길이를 검증한다.
- 공통 `Button`에 로딩 스피너 상태를 추가해 생성·저장·참여·위치 공유·탈퇴·투표 처리 중 버튼 텍스트 대신 스피너를 표시한다.
- 사용자 화면과 안내 문구의 용어를 `그룹 → 크루`, `오너 → 크루장`, `그룹원·구성원 → 멤버`로 통일하고 Firebase 내부 식별자는 호환성을 위해 유지했다.
- 긴 그룹명이 프로필·헤더·홈·기능 페이지의 flex 레이아웃을 밀어내지 않도록 공통 8자 말줄임 규칙과 `min-w-0`/`flex-1` 제약을 적용했다.
- 모든 입력·선택·텍스트 영역의 글자색을 Slate 텍스트 토큰으로 고정하고, viewport·텍스트 자동 확대·터치 동작 설정을 보완해 모바일 확대를 차단했다.
- 그룹 목록과 구성원 프로필 조회를 문서별 개별 요청 대신 30개 단위 `in` 쿼리로 묶어 다중 그룹·구성원 화면 진입 시 Firestore 요청 수를 줄였다.
- 그룹 전환 중 이전 투표 조회가 새 그룹 상태를 덮어쓰지 않도록 요청 순서를 추적하고, 오래된 요청의 로딩·오류 상태 반영을 차단했다.
- 위치 공유 hook의 callback과 컨트롤러 반환 객체를 메모이즈해 위치 상태가 바뀔 때 FamilyProvider 하위 전체가 불필요하게 재렌더링되는 것을 줄였다.
- 채팅 페이지 진입 시 동일한 그룹 구성원 목록을 두 번 조회하던 중복 effect를 제거해 초기 Firestore 요청과 불필요한 재렌더링을 줄였다.
- 로그인 완료 시 중복 실행되던 사용자 프로필 동기화를 인증 상태 콜백 한 번으로 통합하고, 프로필 저장 오류를 인증 오류 상태로 안전하게 전달하도록 개선했다.
- 초대 링크 로그인 시 초기 그룹 목록을 중복 조회하지 않도록 pending 초대 처리와 일반 그룹 목록 로딩을 분리했다.
- `App.tsx`의 페이지 정적 import를 라우트 지연 로딩으로 전환해 초기 엔트리 JS를 약 173KB에서 48KB로 줄이고, 기능 페이지별 청크 로딩으로 초기 진입 부담을 낮췄다.
- GitHub Actions Functions 에뮬레이터 테스트가 CI 초기화 지연으로 기본 15초 제한을 넘지 않도록 테스트·hook 제한을 30초로 늘리고 미러 대기 시간을 25초로 조정했다.
- Firebase Functions 멤버십 미러 트리거를 Firestore 서울 리전(`asia-northeast3`)으로 명시해 기본 미국 리전 실행에 따른 지연을 줄이도록 정리했다.
- Firebase Functions 배포 workflow의 Node 버전을 Functions 런타임(`20`)과 일치시켜 CI 실행 환경 차이 경고를 제거했다.
- 홈 오너 카드의 좌측 장식 아이콘을 제거하고, 그룹명 우측에 초대코드·코드 복사·그룹 설정을 배치했다. 하단 액션은 초대 링크 복사와 시스템 공유가 구분되도록 `링크 복사`·`공유하기`로 정리했다.
- 홈 그룹 설정 아이콘이 내 정보의 `그룹` 탭으로 바로 이동하고, 탭에 자동 포커스와 스크롤을 적용했다.
- 오너 그룹 삭제 시 초대 인덱스가 없는 오래된 그룹도 삭제할 수 있도록 확인 후 삭제하고, Realtime Database 미러 정리는 본 삭제를 막지 않는 best-effort 처리로 보완했다.
- Firestore 규칙 최신본을 Firebase 운영 프로젝트에 게시해 오너의 그룹·구성원 일괄 삭제 권한을 반영했다.
- 홈의 `다른 그룹 추가`를 기존 위치의 독립 카드 UI로 정리하고, 그룹원일 때 본인 구성원 행에 `그룹 나가기` 아이콘과 확인 다이얼로그를 제공했다.
- PWA 웹의 OS 강제 종료 후 백그라운드 위치 추적 한계를 진단 화면에 표시하고, 활성 앱·백그라운드 탭 중심의 위치 공유 정책을 문서화했다.
- 홈 초대코드 영역의 제목을 `새 그룹 참여`로 정리하고, 기존 그룹을 유지한 채 새 그룹을 추가하는 흐름이 드러나도록 안내 문구를 보완했다.
- 홈 그룹 오너 카드의 설정 아이콘을 그룹명 영역 안에 배치하고, 초대코드와 `공유 아이콘 + 초대하기`를 동일한 버튼 스타일의 2분할 전체 너비 액션으로 정리했다.
- 초대 링크 로그인 후 본인 멤버십 문서가 아직 없는 상태에서도 권한 오류가 발생하지 않도록 본인 조건이 포함된 멤버십 쿼리로 중복 참여 여부를 확인하고 규칙 테스트를 보완했다.
- 오류 수정 전 로그인만 완료된 초대 사용자의 pending 초대 코드를 앱 재진입 시 자동 재처리하고, 초대 화면에 `다시 그룹 참여하기` 복구 액션을 추가했다.
- 홈 그룹 오너 카드에서 설정 아이콘은 상단 우측에 유지하고, 초대코드와 `공유 아이콘 + 초대하기` 버튼을 설명 아래로 배치해 그룹 정보가 좁아지지 않도록 정리했다.
- 채팅방 투표 만들기를 전체 ActionLayer 대신 공통 BottomSheet으로 변경해 채팅방을 이탈하지 않고 기존 투표 작성 UI를 사용할 수 있게 했다.
- 그룹 오너 카드에 초대 링크 공유 액션을 추가하고 `/invite/:코드` 진입 화면에서 Google 로그인·가입 후 자동으로 그룹에 참여하도록 연결했다.
- 로그인 redirect에서도 초대 코드를 유지하도록 pending invite 저장을 추가하고, 이미 참여한 구성원은 중복 가입 없이 바로 완료 처리한다.
- 그룹 삭제 시 초대 인덱스가 남아 재가입되는 문제를 막기 위해 오너의 초대 인덱스 삭제 규칙과 테스트를 추가했다.
- 홈 사이드바의 중복 그룹 생성하기 버튼을 제거하고 헤더 그룹 전환 바텀시트에 그룹 생성 폼을 추가했다. 생성 직후 새 그룹으로 전환하고 성공 토스트를 표시한다.
- 위치 공유를 `watchPosition` 기반으로 확장해 이동이 감지될 때만 최소 20m 기준으로 10초마다 Realtime Database에 갱신하고, 화면 재진입 시 최신 위치 동기화를 시도한다.
- 카카오 지도 SDK의 `services` 라이브러리와 역지오코더를 연결해 위치 핀 상세에서 좌표 대신 도로명 주소를 우선 표시한다.
- 웹 PWA 특성상 OS가 브라우저를 강제 정지한 경우에는 백그라운드 GPS 실행을 보장할 수 있어 후속 검토 항목으로 기록했다.
- 위치 공유 컨트롤러를 `FamilyProvider`로 이동해 홈에서 채팅·캘린더 등 다른 화면으로 이동해도 이동 감지 추적이 유지되도록 했다.
- Admin SDK Membership Mirror 운영 전환을 위해 Functions build·emulator test·deploy를 수동 실행하는 GitHub Actions workflow와 Blaze 전환 runbook을 추가했다.
- 홈 사이드영역 그룹 오너 카드의 다크모드 보더를 어두운 Emerald/Slate 대비값으로 분리했다.
- TODO의 Admin SDK Membership Mirror Sync를 재점검하고 Functions 에뮬레이터 생성·수정·삭제 동기화 테스트 통과 상태를 기록했다.
- 다크 테마의 neutral 계열 기본 텍스트 컬러를 Slate 계열로 변경해 전 플랫폼 텍스트 색상 체계를 통일했다.
- 홈 `이달의 일정` 리스트 메타 정보의 날짜·반복·휴무 표기를 굵지 않은 일반 두께로 통일했다.
- 홈 초대코드 입력 카드 아래에 그룹 생성하기 링크를 추가했다.
- 데스크톱 채팅 사이드바를 `진행 중인 채팅` 목록 카드와 `채팅방 만들기` 생성 카드로 분리했다.
- 채팅방 생성 카드와 모바일 생성 레이어에서 불필요한 그룹 초대코드 영역을 제거했다.
- 낮은 브라우저 높이에서도 두 사이드바 카드가 하나의 내부 스크롤 영역으로 동작하도록 조정했다.
- 데스크톱 헤더 주요 메뉴에서 아이콘을 제거하고 텍스트 메뉴만 표시하도록 변경했다.
- 헤더 우측 프로필·알림·로그아웃 영역 간격을 `1rem`으로 통일하고 모바일 로그아웃 숨김은 유지했다.
- 채팅방 생성 영역의 그룹 초대 코드 아이콘을 제거해 타이틀과 코드 중심의 간결한 UI로 정리했다.
- 데스크톱 공통 사이드바와 채팅 사이드바에 브라우저 높이 기반 최대 높이와 내부 스크롤을 적용했다.
- 데스크톱 채팅 사이드바의 `진행 중인 채팅` 아래에 채팅방 생성 영역을 하나의 카드 흐름으로 통합했다.
- 초대코드 영역과 생성 폼 사이에 구분선을 추가하고 비밀방·그룹방을 설정 화면과 동일한 SegmentedControl 탭으로 분리했다.
- 모바일 채팅방 생성은 기존 전체 레이어를 유지하면서 데스크톱 전용 생성 플로팅 버튼 노출을 제거했다.
- 모바일·데스크톱 상단 헤더 제목을 페이지 메뉴명(`채팅`, `투표`, `메모`, `캘린더`)으로 통일하고 콘텐츠 내부 진행 상태 제목은 유지했다.
- 공통 데스크톱 사이드바를 콘텐츠 높이에 맞추도록 `self-start` 기준으로 통일하고 채팅 사이드바의 불필요한 전체 높이 확장을 제거했다.
- 캘린더 날짜 셀에서 날짜 숫자는 왼쪽, 연휴명 배지는 오른쪽으로 정렬했다.
- 데스크톱 헤더 네비게이션 메뉴 묶음을 가로 중앙 정렬로 변경했다.
- 공통 헤더 안쪽을 콘텐츠와 같은 `max-w-screen-2xl` 컨테이너로 감싸고 좌우 패딩 기준을 일치시켰다.
- 채팅 리스트 프로필을 1:1 대화 기준 `36px` 원형 아바타로 통일하고 진행 중인 채팅 헤더의 검색 액션을 수직 중앙 정렬했다.
- 그룹방 생성에 구성원 전체 선택 체크박스를 추가하고 개별 선택 상태와 전체 선택 상태를 연동했다.
- 홈 그룹 위치의 초기화 텍스트 버튼을 아이콘 버튼으로 변경했다.
- 캘린더 연휴 셀의 배경색을 구분하고 날짜 숫자 우측에 연휴명 배지를 `10px`로 표시했다.
- 데스크톱 헤더 주요 메뉴를 그룹 전환 영역 오른쪽 `80px` 간격의 좌측 정렬로 조정했다.
- 채팅방 만들기 레이어를 그룹 초대 코드·비밀방·그룹방 3개 공통 카드로 구성했다.
- 비밀방 생성 시 함께할 그룹 구성원을 선택하고 선택된 `memberIds`를 Firestore에 저장하도록 확장했다.
- 채팅방 목록의 중복 생성 아이콘을 제거하고 데스크톱·모바일 공통 채팅방 생성 플로팅 버튼을 추가했다.
- 페이지별 헤더·좌측 제목을 진행 상태가 드러나는 `진행 중인 채팅`, `진행 중인 투표`, `최근 메모`, `이번 달 캘린더` 문구로 통일했다.
- 채팅 목록·메시지 영역 내부 스크롤바를 숨기고 선택 채팅방을 공통 활성 배경색으로 표시했다.
- 공통 Avatar와 채팅 프로필을 Google `photoURL` 기반 원형 아바타와 인셋 블랙 4% 링으로 통일했다.
- 채팅 좌측 영역의 방 생성 폼을 데스크톱·모바일 공통 전체 레이어로 이동해 목록 화면의 정보 밀도를 낮췄다.
- 채팅 목록을 검색·필터·1:1 대화·채팅방 탐색 중심으로 재구성하고 설정 중복 아이콘과 1:1 이메일 보조 정보를 제거했다.
- 비밀방·그룹방 생성 레이어에 공통 섹션 헤딩과 설명을 적용해 목적과 입력 흐름을 명확히 했다.
- 1:1 대화와 채팅방 행의 패딩·선택 상태·아바타를 디자인 시스템 기준으로 정리했다.
- 데스크톱 하단 네비게이션을 숨기고 그룹명 오른쪽 헤더에 아이콘·텍스트 주요 메뉴를 배치했다.
- 모바일 하단 네비게이션과 데스크톱 헤더 메뉴가 같은 공통 항목·아이콘 데이터를 사용하도록 정리했다.
- 헤더의 데스크톱 좌우 패딩과 콘텐츠 컨테이너 정렬 기준을 `32px`로 유지하도록 통일했다.
- 데스크톱 채팅 워크스페이스를 `100dvh` 기준으로 고정해 좌측 채팅방 목록과 우측 메시지 목록만 내부 스크롤되도록 수정했다.
- 채팅 입력 영역을 고정된 flex 하단 영역으로 유지해 메시지가 길어져도 입력창이 화면 아래로 밀리지 않도록 보완했다.
- 채팅 상세의 투표 만들기 ActionLayer가 데스크톱에서 숨겨지던 문제를 수정해 전체 화면 레이어로 열리도록 확장했다.
- 데스크톱 공통 `DesktopWorkspace` 레이아웃을 추가해 홈, 채팅 외 주요 1뎁스 화면을 좌측 패널·우측 콘텐츠 구조로 통일했다.
- 홈 데스크톱을 좌측 그룹 상황·그룹 관리, 우측 그룹 위치·최근 콘텐츠·그룹 구성원 순서의 단일 열 구조로 재배치했다.
- 최근 일정·메모·투표·채팅 콘텐츠는 모바일 스와이프를 유지하고, 데스크톱에서는 3분할 그리드로 표시하도록 반응형 동작을 분리했다.
- 공통 헤더의 좌우 패딩과 콘텐츠 컨테이너 폭을 맞춰 로고·프로필 라인이 콘텐츠 시작·끝점과 정렬되도록 조정했다.
- 프로필과 설정 화면에도 동일한 데스크톱 좌측 패널·우측 콘텐츠 레이아웃을 적용했다.
- Kakao Map JavaScript 키 Secret이 누락된 배포를 조기에 감지하도록 GitHub Actions 검증 단계를 추가했다.
- Kakao Map 핀의 잘못된 `--color-brand` 토큰을 `--color-primary`로 수정해 핀이 보이지 않던 문제를 해결했다.
- Realtime Database 위치값을 좌표 범위와 숫자 형식 기준으로 검증해 잘못된 위치 한 건이 전체 지도를 깨뜨리지 않도록 했다.
- Kakao Map SDK 로딩 실패를 기본 위치 영역과 안내 문구로 표시하고, 실패 후 재시도 가능한 로딩 구조로 보완했다.
- GitHub Actions Hosting 빌드에 `VITE_KAKAO_MAP_JAVASCRIPT_KEY` Secret 주입을 추가했다.
- Admin SDK membership mirror 동기화용 Cloud Functions 코드베이스를 추가했다.
- `familyMembers` 생성·수정·삭제 시 RTDB mirror를 생성·갱신·삭제하는 트리거를 추가했다.
- Functions emulator에서 Firestore → RTDB 동기화를 검증하는 테스트를 추가했다.
- Blaze 전환 전까지 클라이언트 mirror 쓰기를 유지하도록 운영 정책을 문서화했다.
- 다중 그룹 환경에서 사용자가 그룹별 OWNER/MEMBER 역할에 따라 구성원 역할 변경·삭제·OWNER 승격을 분리할 수 있는지 Firestore 규칙 테스트를 추가했다.
- Realtime Database membership mirror도 그룹별 OWNER 쓰기 권한이 서로 섞이지 않는지 규칙 테스트를 추가했다.
- 모바일 채팅 상세를 `top: 64px / bottom: 0` fixed 쉘로 고정해 입력 영역 아래 빈 공간과 전체 페이지 스크롤을 제거했다.
- 과거 메시지를 보고 있는 중에도 텍스트·투표 전송 후 최신 메시지 위치로 강제 부드러운 스크롤이 실행되도록 연결했다.
- 채팅 2뎁스의 바깥 `main`을 `100dvh`와 `overflow-y-hidden`으로 제한해 페이지 전체 스크롤을 차단하고 메시지 목록만 스크롤되도록 고정했다.
- 채팅 메시지 스크롤 컨테이너의 `justify-end`를 내부 `mt-auto` 래퍼로 교체해 긴 대화에서도 스크롤 시작점이 자연스럽게 유지되도록 보정했다.
- 채팅 상세를 헤더 아래 고정 높이의 flex 레이아웃으로 정리해 메시지 영역만 스크롤되도록 변경했다.
- 모바일 채팅 입력창을 메시지 영역을 가리지 않는 하단 composer로 재배치하고 safe-area 여백을 유지했다.
- 채팅방 진입 시 최신 메시지로 이동하고, 사용자가 하단을 보고 있을 때 새 메시지만 부드럽게 자동 스크롤하도록 조정했다.
- 공통 `Button`, `IconButton`, `BottomSheetItem`에 눌림 스케일과 색상 전환 피드백을 추가했다.
- 캘린더·투표·메모·채팅·진단 화면의 체크박스를 애니메이션 체크 스트로크가 있는 공통 `AnimatedCheckbox`로 통일했다.
- 프로필 `MY | 그룹`과 캘린더 `일정 등록 | 날짜 투표` 탭을 슬라이딩 인디케이터가 있는 공통 `SegmentedControl`로 통일했다.
- BottomSheet, 모바일 ActionLayer, Toast에 진입 모션을 추가하고 BottomSheet의 ESC 닫기와 닫힘 애니메이션을 함께 보존했다.
- 캘린더 날짜·일정 표시, 날짜 투표 후보, 투표 선택 항목, 채팅 전송 버튼에 짧은 터치 피드백을 연결했다.

## 2026-09-15

- 그룹 전환·구성원 관리·역할 변경 바텀시트 항목을 공통 `BottomSheetItem`으로 통일했다.
- 채팅 리스트 최근 선택 상태의 링 보더와 강한 배경을 제거하고 은은한 선택 배경만 사용하도록 조정했다.
- 채팅 리스트를 카카오톡식 구조로 정리해 상단 검색·채팅방 생성·설정 액션, 방 유형 필터, 아바타, 마지막 메시지 미리보기, 시간을 추가했다.
- 채팅 리스트 행의 불필요한 카드 배경을 제거하고 선택 상태만 강조해 목록 스캔성을 높였다.
- 캘린더 일정 등록과 날짜 투표 탭의 콘텐츠 시작 여백을 동일하게 맞추고 탭·섹션 아이콘 규칙을 유지했다.
- 공통 `SectionHeading`을 추가해 카드 주요 제목의 아이콘 위치, 제목 크기, 설명 간격을 통일했다.
- 채팅 사이드 카드와 모바일 ActionLayer의 불필요한 제목 하단 구분선을 제거하고 여백 기반으로 섹션을 분리했다.
- 투표 일반·날짜 선택과 캘린더 일정·날짜 투표 탭에 동일한 아이콘 버튼 스타일을 적용했다.
- 메모·캘린더·채팅·투표 카드의 주요 내부 헤더와 여백 기준을 통일했다.
- 홈과 내 정보의 그룹 카드를 현재 역할에 따라 오너·그룹원 UI로 구분했다.
- 오너 카드에만 초대코드 복사와 그룹 관리 진입을 노출하고, 그룹원 카드에서는 초대코드를 숨겼다.
- 그룹원도 별도 초대코드 입력으로 다른 그룹에 참여할 수 있도록 참여 경로는 유지했다.
- 모든 백버튼 아이콘을 Phosphor `CaretLeft`에서 `ArrowLeft` 화살표로 통일했다.
- 다크모드 팔레트를 네이비/슬레이트 계열에서 블랙·차콜 기반 중성 톤으로 변경했다.
- 다크모드의 배경, 표면, 보조 표면, 텍스트, 보더 대비를 블랙 계열 토큰으로 재조정하고 에메랄드 포인트는 유지했다.
- 설정에 OS 기본값을 따르는 다크모드와 직접 라이트/다크 전환 토글, 기기 설정 복귀 동작을 추가했다.
- Slate/Emerald 디자인 토큰의 다크 테마와 주요 고정 색상 유틸리티 오버라이드를 추가해 앱 전체 다크모드 대응을 구성했다.
- 설정 최하단에 회원탈퇴 확인 UI를 추가하고, Google 재인증 후 사용자 프로필·그룹 연결·Firebase Auth 계정을 삭제하도록 연결했다.
- 그룹 오너 계정은 그룹을 먼저 정리하도록 회원탈퇴를 제한하고, 본인 사용자 문서 삭제 Firestore 규칙을 추가했다.
- 내 정보 그룹 탭의 `그룹 생성` 버튼을 그룹 목록 하단으로 이동했다.
- 홈 초대 영역의 입력창을 독립된 전체 너비 필드로 고정하고 버튼과 8px 간격을 유지하도록 분리했다.
- 위치 공유 해제 버튼의 대비를 높여 `red-600` 배경과 흰색 텍스트로 변경했다.
- 캘린더에서 휴무일 일정이 포함된 날짜 셀을 앰버 배경과 인셋 링으로 표시했다.
- 하단 네비게이션 선택 상태의 배경색을 제거하고 브랜드 아이콘·텍스트 색상만으로 표시했다.
- 홈 초대코드 입력과 그룹 참여 버튼을 모든 디바이스에서 8px 간격의 반응형 가로 배열로 조정했다.
- 하단 네비게이션의 safe-area를 내부 높이 증가가 아닌 하단 위치 보정으로 변경했다.
- 하단 네비게이션 스크롤 축소 상태에서 컨테이너·아이템·아이콘을 약 70% 크기로 줄이고 아이콘만 표시하도록 정리했다.
- 내 정보 `그룹` 탭에 새 그룹 생성 버튼과 그룹 이름 입력·생성·취소 폼을 추가했다.
- 위치 공유 해제 버튼이 레드 계열 배경·텍스트를 사용하도록 유지·검증했다.
- 홈 상단 순서를 `오늘의 그룹 상황` 다음 `그룹 초대`가 오도록 조정했다.
- 초대코드 입력과 그룹 참여 버튼을 모바일에서는 세로로 분리하고, 데스크톱에서는 간격 있는 인라인 배치로 정리했다.
- 메인 1뎁스·2뎁스 앱 헤더의 실제 높이를 `64px`로 고정하고 내부 정렬 기준을 통일했다.
- 앱 헤더, Modal, BottomSheet, 모바일 ActionLayer의 제목 폰트를 `18px / 600` 기준으로 통일했다.
- 홈 헤더의 그룹 전환 UI를 기존 BottomSheet 컴포넌트로 통일하고, 그룹 리스트 터치 즉시 전환되도록 변경했다.
- 홈 그룹 구성원 섹션에 명시적인 하단 순서를 부여해 그룹 위치·최근 정보 아래에 표시되도록 정리했다.
- 위치 공유 해제 상태 버튼을 `bg-red-50` / `text-red-500` 스타일로 구분했다.
- 초대 코드 입력과 그룹 참여 버튼을 모바일에서도 한 줄 인라인 그룹으로 고정했다.
- 홈 그룹 초대 영역에서 별도 그룹 관리 버튼을 제거하고 초대 코드 우측 톱니바퀴 아이콘을 그룹 관리 진입점으로 변경했다.
- 홈 위치 공유 버튼을 공유 중에는 `위치 공유 끊기`로 전환하고, RTDB에서 내 위치 데이터를 삭제하는 해제 동작을 추가했다.
- 홈 그룹 참여 입력에서 별도 초대 코드 라벨을 제거하고 `초대코드를 입력해주세요` placeholder와 참여·관리 아이콘을 적용했다.
- 홈 헤더에서 `우리끼리` 텍스트를 제거하고 현재 그룹명과 아래 방향 전환 아이콘만 표시하도록 정리했다.
- 홈 상단에서 그룹 액션 영역과 오늘의 그룹 상황 영역 순서를 교환했다.
- 홈 이달의 일정·최근 정보 스와이프 영역의 우측 하단 점 인디케이터를 제거했다.
- 내 정보 `MY | 그룹` 탭을 캘린더 등록 탭과 동일한 세그먼트 UI로 통일했다.
- 내 정보 화면을 `MY | 그룹` 탭으로 분리했다.
- 홈에 초대 코드 입력과 그룹 참여 동작을 추가했다.
- 우측 헤더 그룹 전환을 제거하고 홈 브랜드 영역의 `우리끼리 > 그룹명...` 전환 UI로 통합했다.
- 내 정보 그룹 영역을 목록·선택·수정·삭제 중심으로 정리하고 생성·참여 UI를 제거했다.
- 홈의 현재 그룹 영역을 `그룹 초대하기`로 표시하고 초대 코드 복사 동작을 유지했으며, 그룹 관리 버튼의 라운드 값을 기본 버튼과 통일했다.
- 홈 상단 그룹 상황 문구에 현재 그룹명을 표시하고 주요 타이틀 크기를 한 단계 낮췄다.
- 내 정보에 그룹 목록과 그룹 생성·초대 참여·이름 수정·삭제·나가기 기능을 추가했다.
- 홈의 그룹 생성·참여 폼을 제거하고 그룹 관리하기 링크로 통합했으며, 헤더 전환 메뉴에는 그룹 목록과 관리 링크만 남겼다.
- 그룹 삭제 시 오너 권한과 구성원 연결을 확인하고, 삭제된 그룹의 초대 코드 재사용을 막도록 검증했다.
- 다중 그룹 기반의 첫 단계로 사용자별 그룹 목록 조회, 현재 그룹 저장 및 헤더 그룹 전환 UI를 추가했다.
- 홈·채팅·투표·메모·캘린더의 Firebase 구독이 선택된 그룹을 기준으로 재연결되도록 변경했다.
- 사용자 노출 문구의 `가족` 용어를 `그룹`으로 통일했다.
- 기존 Firebase `family*` 내부 식별자는 데이터 호환을 위해 유지하고, 다중 그룹 확장 방향을 프로젝트 문서와 TODO에 기록했다.
- 상단 홈 헤더 브랜드를 전달받은 SVG 라운드 스퀘어 로고와 `우리끼리` 텍스트로 변경했다.
- 다이얼로그와 바텀시트 상단 헤더를 64px 높이로 통일하고 하단 보더를 제거했다.
- 구성원 역할 변경을 즉시 저장 방식에서 선택 후 `변경하기`로 확정하는 방식으로 변경했다.
- 홈, 투표, 메모, 채팅, 캘린더, 내 정보의 인라인 알림 영역을 제거하고 토스트 메시지로 통일했다.
- 바텀시트에 모바일 하단 세이프에어리어와 기본 여백을 반영했다.
- 홈 가족 구성원 더보기 버튼의 원형 배경을 제거하고 아이콘만 표시하도록 정리했다.
- 가족 위치 지도 하단에 위치 공유 구성원의 Google 프로필 스트립을 추가했다.
- 가족 위치 프로필 스트립에서 이름을 제거하고 프로필 이미지만 표시하도록 간소화했다.
- 지도 아래 프로필을 누르면 해당 구성원 위치로 지도 중심이 이동하고 확대되도록 연결했다.
- PWA 서비스 워커를 빌드마다 커밋/빌드 시간 기반 캐시 버전으로 자동 변경하도록 구성했다.
- 서비스 워커 등록 시 브라우저 HTTP 캐시를 우회하고 앱 재진입, 포커스, 온라인 복귀 시 업데이트를 확인하도록 보완했다.
- 업데이트 적용 시 활성 워커가 아닌 대기 중인 새 워커에 `SKIP_WAITING` 메시지를 보내도록 수정했다.
- Firebase Hosting에서 `sw.js`와 `index.html`이 최신 버전을 즉시 확인하도록 캐시 금지 헤더를 추가했다.
- 배포 산출물 검증에 서비스 워커 버전 마커 미치환 검사를 추가했다.

## 2026-09-09

- 초기 기획 문서를 `PROJECT_CONTEXT.md`, `TODO.md`, `CHANGELOG.md`, `CODING_RULES.md` 4개 파일로 분리했다.
- Git 저장소 및 서버 호스팅 연동 필요 항목을 `TODO.md`에 추가했다.
- 로컬 Git 저장소를 초기화하고 기본 `.gitignore`를 추가했다.
- 초기 문서 정리 커밋을 생성했다.
- GitHub 저장소 URL, Dothome 호스팅 URL, Firebase 프로젝트 생성 예정 상태를 문서에 반영했다.
- Git 원격 저장소 `origin`을 `https://github.com/recoba00/our_share.git`로 연결했다.
- 로컬 `master` 브랜치를 GitHub `origin/master`로 push했다.
- Firebase CLI가 현재 환경에 설치되어 있지 않음을 확인했다.
- Firebase Storage를 Spark 요금제 MVP 필수 설정에서 제외하고 보류 정책으로 변경했다.
- Firebase Auth Google 로그인, Cloud Firestore 서울 리전, Realtime Database 싱가포르 리전 설정 상태를 문서에 반영했다.
- Firebase Web App 설정값을 로컬 `.env`에 추가하고, Git 공유용 `.env.example`을 생성했다.
- Firebase 프로젝트 ID를 `our-share-6baf5`로 문서에 반영했다.
- Realtime Database 연결에 필요한 `databaseURL` 확인 항목을 `TODO.md`에 추가했다.
- Realtime Database URL을 로컬 `.env`에 추가하고 문서에 반영했다.
- 하단 네비게이션을 `홈 / 채팅 / 투표 / 메모 / 캘린더` 구조로 재정의했다.
- 화면 기획서 `SCREEN_SPEC.md`와 기능 정의서 `FEATURE_SPEC.md` 초안을 추가했다.
- 채팅 내 가족 초대, 비밀방, 투표 생성/전송 흐름과 메모/캘린더 세부 기능을 문서화했다.
- Tailwind, Pretendard, Phosphor Icons, 4pt Grid System, 반응형 기준, Slate/Emerald 기반 디자인 토큰 정책을 문서화했다.
- Figma 디자인 연동 전에는 Codex가 기본 디자인 시스템을 구축하고, 추후 토큰/공통 컴포넌트 단위로 교체하는 방침을 추가했다.
- React + TypeScript + Vite 앱 골격을 추가했다.
- Tailwind CSS, Pretendard, Phosphor Icons, Firebase, React Router 의존성을 추가했다.
- Slate/Emerald 기반 CSS Variable 디자인 토큰과 Tailwind theme token 연결을 구성했다.
- `홈 / 채팅 / 투표 / 메모 / 캘린더` 하단 네비게이션과 반응형 앱 Shell을 구현했다.
- Firebase Auth, Firestore, Realtime Database 초기화 모듈과 Google 로그인 서비스 초안을 추가했다.
- `npm run build`와 `npm run lint` 검증을 통과했다.
- 앱 전역 `AuthProvider`와 `useAuth` 훅을 추가해 Firebase 로그인 상태를 구독하도록 구성했다.
- Google 로그인/로그아웃 UI와 사용자 `users` 문서 동기화를 연결했다.
- Firestore 기반 가족 생성, 초대 코드 생성, 초대 코드 참여 서비스를 추가했다.
- 홈 화면에 로그인 전 시작 화면과 로그인 후 가족 생성/초대 코드 참여 폼을 연결했다.
- Realtime Database 기반 위치 공유 서비스와 `liveLocations/{familyId}/{userId}` 쓰기 흐름을 추가했다.
- 브라우저 Geolocation 권한 요청, 현재 위치 조회, 배터리 상태 조회를 홈 화면의 현재 위치 공유 액션에 연결했다.
- `npm run lint`, `npm run build`, 로컬 브라우저 렌더링 검증을 통과했다.
- Dothome에서 개발용 루트 `index.html`이 서빙되어 화면이 보이지 않는 문제를 진단했다.
- Dothome 배포는 프로젝트 루트가 아니라 `dist` 폴더 내용만 업로드해야 함을 `DEPLOYMENT.md`에 문서화했다.
- SPA 라우팅을 위해 `public/.htaccess`를 추가했다.
- GitHub Actions 배포 workflow를 `npm ci`, `npm run build`, `dist` FTP 업로드 순서로 수정했다.
- GitHub Actions 빌드에 필요한 Firebase/Vite secrets 목록을 `DEPLOYMENT.md`에 추가했다.
- 로그인 필요 화면을 위한 `RequireAuth` 컴포넌트를 추가하고 채팅/투표/메모/캘린더 라우트에 적용했다.
- 가족 구성원 목록 조회 서비스를 추가하고 홈 화면에 가족 구성원 프로필 목록을 표시하도록 연결했다.
- 공통 `Avatar` 컴포넌트를 추가했다.
- Firestore 기반 투표 생성, 목록 조회, 투표 참여, 결과 집계 서비스를 추가했다.
- 투표 화면에 일반/날짜 투표 생성, 단일/복수 선택, 결과 퍼센트 표시 UI를 연결했다.
- 투표 채팅방 전송 버튼은 후속 Chat 연동 대상으로 남겨두었다.
- `npm run lint`와 `npm run build` 검증을 통과했다.
- Firestore 기반 채팅방/메시지 타입과 서비스를 추가했다.
- 채팅 화면에 가족 전체방 자동 생성, 채팅방 실시간 목록 구독, 메시지 실시간 구독, 텍스트 메시지 전송을 연결했다.
- 채팅 화면에 가족 초대 코드 표시와 비밀방 생성 흐름을 추가했다.
- 채팅 MVP는 Storage 없이 텍스트 메시지만 지원하도록 유지했다.
- `npm run lint`, `npm run build`, 로컬 `/our_share/chat` 응답 검증을 통과했다.
- Dothome 서버의 `our_share` 폴더를 비운 뒤 GitHub Actions 배포를 다시 트리거했다.
- 배포된 HTML/JS/CSS가 Dothome에서 200으로 내려오는 것을 확인했다.
- 배포 JS에 Firebase 환경변수가 주입되지 않은 상태를 확인하고, GitHub Actions에 Firebase build secrets 누락 검증 단계를 추가했다.
- Firebase Web App 설정값을 `src/lib/firebase/app.ts`의 기본 fallback으로 추가해 GitHub Secrets 없이도 Dothome 정적 배포가 동작하도록 수정했다.
- Firebase 설정 fallback 도입에 맞춰 GitHub Actions의 Firebase build secrets 필수 검증 단계를 제거했다.
- GitHub Actions의 미등록 Secret이 빈 문자열로 주입되는 경우에도 Firebase 기본 설정을 사용하도록 fallback 조건을 보완했다.
- Firestore 기반 메모 타입과 서비스를 추가했다.
- 메모 화면에 가족별 메모 실시간 목록, 일반 메모 생성, 민감 메모 생성, 선택 메모 상세 열람 흐름을 연결했다.
- 민감 메모 본문은 개인 비밀번호 기반 Web Crypto AES-GCM 암호화 후 저장하고, 열람 시 비밀번호로 복호화하도록 구성했다.
- `visibleTo` 기반 민감 메모 표시 필터를 추가했다.
- `npm run lint`, `npm run build`, 로컬 `/our_share/memo` 응답 검증을 통과했다.
- Firestore 기반 캘린더 일정 타입과 서비스를 추가했다.
- 캘린더 화면에 가족별 일정 실시간 목록, 월간 그리드, 일정 등록/수정/삭제 흐름을 연결했다.
- 매년 반복 일정과 휴무일 체크 옵션을 `calendarEvents` 데이터에 저장하고 월간 캘린더에 표시하도록 구현했다.
- 기념일/생일 분류를 일정 카테고리로 선택할 수 있게 추가했다.
- `npm run lint`, `npm run build`, 로컬 `/our_share/calendar` 응답 검증을 통과했다.
- 홈 대시보드의 목업 일정/메모/투표/채팅 요약을 실제 Firebase 데이터 구독 기반으로 교체했다.
- 홈에서 가족 구성원, 이달의 캘린더 일정, 최근 메모, 최근 투표, 최근 채팅방 상태를 표시하도록 연결했다.
- 투표 목록을 홈에서 실시간으로 재사용할 수 있도록 `subscribePolls` 서비스를 추가했다.
- `npm run lint`, `npm run build`, 로컬 `/our_share/` 응답 검증을 통과했다.
- PWA manifest, SVG 앱 아이콘, 서비스 워커, 서비스 워커 등록 모듈을 추가했다.
- 앱 HTML에 manifest, favicon, apple touch icon, description 메타 정보를 연결했다.
- Dothome HTTP 접속에서는 서비스 워커/PWA 설치가 제한될 수 있음을 `DEPLOYMENT.md`에 문서화했다.
- 투표 화면에 채팅방 선택 UI를 추가하고 기존 투표를 선택한 채팅방으로 전송하는 흐름을 연결했다.
- 채팅 메시지에 `POLL` 타입 전송 서비스를 추가했다.
- 진단 저장 권한 검사에 Realtime Database `liveLocations/{familyId}/{userId}` 위치 공유 쓰기 및 자동 삭제 검사를 추가했다.
- MVP 릴리즈 체크리스트에 RTDB 위치 쓰기 진단 항목을 추가했다.
- 채팅 화면에서 투표 메시지를 카드 형태로 표시하고 투표 화면으로 이동할 수 있게 연결했다.
- 채팅 화면에 투표 생성 진입 버튼을 추가했다.
- `npm run lint`, `npm run build`, 로컬 `/our_share/poll`, `/our_share/chat` 응답 검증을 통과했다.
- 채팅방 진입 시 상대 메시지를 `readBy` 배열에 자동 추가하는 읽음 처리 서비스를 구현했다.
- 채팅 메시지 하단에 내 메시지 읽음 인원 수와 수신 메시지 읽음 상태를 표시했다.
- `npm run lint`, `npm run build`, 로컬 `/our_share/chat` 응답 검증을 통과했다.
- Firestore 보안 규칙 초안 `firestore.rules`를 추가했다.
- Realtime Database 보안 규칙 초안 `database.rules.json`을 추가했다.
- Firebase Rules 배포용 `firebase.json`과 콘솔 적용 안내 `SECURITY_RULES.md`를 추가했다.
- RTDB Rules는 Firestore membership을 직접 참조할 수 없으므로 가족 단위 위치 읽기 제한을 후속 membership mirror 작업으로 남겼다.
- 가족 생성/초대 참여 시 Realtime Database `familyMembers/{familyId}/{userId}` mirror를 함께 기록하도록 추가했다.
- RTDB `liveLocations`, `onlinePresence`, `deviceStatus` 읽기/쓰기 조건을 membership mirror 기준으로 강화했다.
- 클라이언트 기반 membership mirror는 MVP용이며 운영 보안 경계로는 Admin SDK 또는 Cloud Functions 기반 동기화가 필요함을 문서화했다.
- 기존 사용자에 대한 RTDB membership backfill과 서버 권한 mirror 동기화는 후속 작업으로 남겼다.

## 2026-09-10

- 캘린더 날짜 계산 유틸을 `features/calendar/utils`로 분리했다.
- 캘린더 월간 그리드와 일정 목록에 D-Day 배지를 표시하도록 추가했다.
- 매년 반복 일정은 올해 날짜가 지났으면 다음 해 기준으로 D-Day를 계산하도록 구성했다.
- 홈 대시보드의 이달 일정 요약에도 D-Day 정보를 표시하도록 연결했다.
- 캘린더 화면에서 날짜 후보를 입력해 `DATE` 타입 투표를 바로 생성하는 Calendar Vote 흐름을 추가했다.
- 캘린더 MVP 하위 작업 완료에 맞춰 Phase 08 Calendar 체크 상태를 완료로 갱신했다.
- Google 로그인 popup 실패 원인을 화면에 표시하도록 AuthProvider와 로그인 UI를 보완했다.
- popup 차단 또는 중복 popup 요청 시 Firebase redirect 로그인으로 fallback되도록 인증 서비스를 개선했다.
- Firebase Auth 승인 도메인 누락, Google provider 비활성화, popup 조기 종료에 대한 사용자 안내 메시지를 추가했다.
- 홈 화면에서 Realtime Database `liveLocations/{familyId}`를 구독해 가족별 위치 상태를 표시하도록 연결했다.
- 가족 위치 카드에 좌표, 업데이트 시각, 정확도, 배터리/충전 상태를 표시하는 Family Pins MVP UI를 추가했다.
- 가족 위치 카드에서 `어디야?`, `언제 와?`, `오는 길에 마트 들러줘!` 빠른 메시지를 가족 전체방으로 전송하는 Quick Message 기능을 추가했다.
- Location MVP 하위 작업 완료에 맞춰 Phase 07 Map / Location 체크 상태를 완료로 갱신했다.
- 가족 OWNER가 홈의 가족 구성원 목록에서 `PARENT`, `MEMBER`, `CHILD` 역할을 변경할 수 있는 Member Permissions MVP를 추가했다.
- 역할 변경 시 Firestore `familyMembers` 문서와 Realtime Database membership mirror를 함께 갱신하도록 연결했다.
- Firestore/Realtime Database 보안 규칙 초안에서 가족 역할 변경을 OWNER 중심으로 제한하도록 보완했다.
- Realtime Database 위치 validate 규칙에서 `accuracy` null 값을 허용하도록 클라이언트 쓰기 구조와 맞췄다.
- Family MVP 하위 작업 완료에 맞춰 Phase 05 Family 생성 / 초대 체크 상태를 완료로 갱신했다.
- 공통 `Modal` 컴포넌트를 추가하고 Escape/배경 클릭 닫기, footer 슬롯, 접근성 role을 지원하도록 구성했다.
- 모바일 중심 `BottomSheet` 컴포넌트를 추가하고 데스크톱에서는 중앙 시트처럼 보이도록 반응형 스타일을 적용했다.
- 앱 전역 `ToastProvider`와 `useToast` 훅을 추가해 success/error/info 알림을 어디서든 띄울 수 있게 구성했다.
- Design System / Common UI 하위 작업 완료에 맞춰 Phase 02 체크 상태를 완료로 갱신했다.
- 채팅 서비스에 가족 구성원 간 1:1 `DIRECT` 채팅방 자동 생성 기능을 추가했다.
- 채팅 서비스에 선택한 가족 구성원만 참여하는 `PRIVATE_GROUP` 채팅방 생성 기능을 추가했다.
- 채팅 화면에서 가족 구성원 목록 기반 1:1 대화 시작과 그룹방 멤버 선택 UI를 추가했다.
- 채팅방 목록에 가족/1:1/그룹 타입 표시를 추가했다.
- Firestore 보안 규칙 초안에서 채팅방과 메시지 접근을 방 멤버 기준으로 제한하도록 보완했다.
- Chat MVP 하위 작업 완료에 맞춰 Phase 12 Chat 체크 상태를 완료로 갱신했다.
- 브라우저 알림 권한 요청, 지원 여부 확인, 중복 알림 방지 유틸을 `features/notification`에 추가했다.
- 홈 화면에 오늘 일정과 24시간 내 마감 투표를 브라우저 알림으로 확인하는 MVP 버튼을 추가했다.
- 알림 권한이 이미 허용된 경우 홈 데이터 갱신 시 대시보드 알림을 자동 평가하도록 연결했다.
- Memo 상위 Phase 체크 상태를 세부 완료 항목과 맞춰 완료로 정리했다.
- Notification MVP 하위 작업 완료에 맞춰 Phase 11 Notification 체크 상태를 완료로 갱신했다.
- MVP 100% 완료 후 Dothome에서 Firebase Hosting으로 이전하는 호스팅 전략을 `PROJECT_CONTEXT.md`에 추가했다.
- Firebase Hosting 이전 시 필요한 SPA rewrite, Vite base 검토, GitHub Actions 배포 대상 교체 작업을 `TODO.md`에 추가했다.
- 초대 코드 단건 조회용 `familyInvites` 컬렉션을 추가하고, 가족 생성 시 초대 인덱스를 함께 생성하도록 연결했다.
- 초대 코드 참여 흐름을 `families` 전체 조회에서 `familyInvites/{inviteCode}` 단건 조회 방식으로 변경했다.
- 기존 가족 OWNER가 가족 정보를 불러올 때 누락된 초대 인덱스를 자동 보강하도록 추가했다.
- 가족 정보를 불러올 때 로그인 사용자의 RTDB membership mirror를 보강해 기존 사용자 위치 공유 호환성을 개선했다.
- Firestore Rules에서 `families` 전체 read를 차단하고 가족 구성원만 가족 문서를 읽도록 강화했다.
- Firestore Rules에서 family member 생성, 일정/메모/투표/채팅/메시지/투표값의 필드 크기와 update bypass 방어 조건을 보강했다.
- Realtime Database Rules에서 membership mirror 역할 쓰기와 위치 좌표 validate 조건을 강화했다.
- `SECURITY_RULES.md`에 `familyInvites` 기반 초대 정책과 RTDB mirror backfill MVP 방식을 문서화했다.
- Firebase CLI `15.30.0`으로 `our-share-6baf5` 프로젝트에 Firestore Rules와 Realtime Database Rules를 실제 게시했다.
- Firebase CLI 기본 프로젝트를 명시하기 위해 `.firebaserc`를 추가했다.
- Rules 게시 완료에 맞춰 `TODO.md`의 Firebase Console Rules Publish 항목을 완료로 갱신했다.
- Firestore Security Rules 자동 테스트를 위해 `@firebase/rules-unit-testing`, `vitest` devDependency와 `npm run test:rules` 스크립트를 추가했다.
- 가족 가입, 가족 문서 읽기, 채팅 메시지, 채팅방 멤버 변조, 투표 대리 작성 방지 시나리오를 검증하는 `tests/firestore.rules.test.ts`를 추가했다.
- Firebase emulator 실행에 Java Runtime이 필요하며 현재 환경에서는 `java -version` 실행 실패로 rules 테스트 실행이 보류됨을 문서화했다.
- Android Studio에 포함된 JBR을 자동 감지하는 `scripts/run-firestore-rules-tests.mjs`를 추가해 전역 Java 없이도 rules 테스트를 실행할 수 있게 했다.
- `npm run test:rules`로 Firestore emulator 기반 Security Rules 테스트 8개가 모두 통과함을 확인했다.
- Firestore Rules Test 완료에 맞춰 Phase 13 Security 강화를 완료로 갱신하고, Admin SDK membership mirror 동기화는 Post-MVP 항목으로 분리했다.
- MVP 자동/수동 검증 항목을 추적하기 위해 `MVP_RELEASE_CHECKLIST.md`를 추가했다.
- Dothome 배포 URL, GitHub Actions FTP 배포, `dist` 업로드가 실제 동작 확인된 상태에 맞춰 `TODO.md` 체크 상태를 정리했다.
- Dothome Apache 환경에서 PWA manifest와 정적 자산 MIME 타입이 명확히 내려오도록 `public/.htaccess`에 `AddType` 설정을 추가했다.
- Firebase 구독 오류 메시지를 공통으로 처리하는 `getFirebaseErrorMessage` 유틸을 추가했다.
- 캘린더, 채팅, 메모, 투표, 위치 구독 함수에 오류 콜백을 추가해 권한/연결 오류가 화면 상태 메시지로 드러나도록 보완했다.
- Dothome manifest MIME type 확인 상태를 `TODO.md`와 `MVP_RELEASE_CHECKLIST.md`에 맞춰 완료로 정리했다.

## 2026-09-11

- React 렌더링 오류가 발생해도 빈 화면으로 멈추지 않도록 공통 `ErrorBoundary` 컴포넌트를 추가했다.
- 앱 루트의 `AppLayout`과 라우트 전체를 `ErrorBoundary`로 감싸고, 새로고침/홈 이동 복구 버튼을 제공하도록 연결했다.
- MVP 릴리즈 체크리스트에 런타임 화면 오류 fallback 검증 항목을 추가했다.
- 로그인 전에도 배포/Firebase/브라우저/위치 지원 상태를 확인할 수 있는 `/diagnostics` 진단 화면을 추가했다.
- MVP 릴리즈 체크리스트에 진단 화면 수동 확인 항목을 추가했다.
- GitHub Actions 배포 빌드에 `VITE_BUILD_COMMIT`, `VITE_BUILD_TIME`을 주입하도록 설정했다.
- `/diagnostics` 화면의 호스팅 섹션에 배포 커밋과 빌드 시각을 표시하도록 추가했다.
- `/diagnostics` 화면에 브라우저 `localStorage` 기반 MVP 수동 테스트 체크 패널과 진행률 표시를 추가했다.
- 투표, 메모, 캘린더, 채팅방 생성이 Rules에서 허용되는지 확인하는 MVP create-flow Security Rules 테스트를 추가했다.
- 실제 로그인 계정으로 캘린더, 메모, 투표, 채팅방 저장 및 자동 삭제를 시도하는 `/diagnostics` 저장 권한 검사 패널을 추가했다.
- 저장 실패 원인 확인을 위해 Firestore/Realtime Database Rules를 `our-share-6baf5` 프로젝트에 재배포했다.
- 캘린더, 메모, 투표, 투표값, 채팅방, 메시지 저장 경로를 `families/{familyId}` 하위 컬렉션으로 변경해 Firestore Rules가 가족 단위 목록 조회를 안정적으로 허용할 수 있게 정리했다.
- 채팅/투표/메모/캘린더 저장 후 목록 갱신이 막히는 문제를 재현하는 Security Rules 테스트를 앱 query 형태로 보강했다.
- 가족 전체방과 1:1 채팅방을 생성하기 전에 클라이언트가 deterministic room id로 존재 여부를 확인할 수 있도록, 존재하지 않는 가족 하위 `chatRooms` 문서의 `get` 권한만 허용하는 Firestore Rules 테스트를 추가했다.
- 투표 화면에서 채팅방 자동 확인 실패가 투표 생성 버튼 비활성화로 이어지지 않도록 가족 상태 세팅과 채팅방 준비 흐름을 분리했다.
- 채팅 화면에서 가족 전체방 자동 확인 실패가 가족 구성원 목록과 1:1 채팅 시작 UI를 막지 않도록 초기화 흐름을 보강했다.
- `/diagnostics` 저장 권한 검사에 가족 전체방 자동 준비와 1:1 채팅방 준비 검사를 추가해 채팅방 초기화 권한 문제를 사전에 확인할 수 있게 했다.
- Firestore Rules 배포 로그의 미사용 함수 경고를 줄이기 위해 사용하지 않는 읽기 헬퍼 함수를 제거했다.
- Dothome 배포 상태를 자동 확인할 수 있도록 주요 SPA 라우트와 JS/CSS 자산, 배포 커밋 포함 여부를 검사하는 `npm run test:hosting` 스모크 스크립트를 추가했다.
- GitHub Actions Dothome 배포 workflow에 배포 후 `npm run test:hosting` 검증 단계를 추가하고, FTP 반영 지연에 대비한 재시도 옵션을 스모크 스크립트에 추가했다.
- GitHub Actions Dothome 배포 workflow에 배포 전 `npm run lint`와 `npm run test:rules` 검증 단계를 추가하고, Firebase emulator 실행을 위한 Java 설정을 추가했다.
- Firestore Rules 테스트 실행 스크립트가 Windows와 Linux 모두에서 PATH를 올바르게 구성하도록 OS별 경로 구분자를 사용하게 수정했다.
- `npm run test:rules`가 Firestore와 Realtime Database 에뮬레이터를 함께 실행하도록 확장했다.
- Realtime Database `familyMembers` mirror와 `liveLocations` 읽기/쓰기 권한을 검증하는 `tests/database.rules.test.ts`를 추가했다.
- Realtime Database는 null 필드를 저장하지 않는 특성을 반영해 `liveLocations` validate 규칙에서 `accuracy`를 필수 child 목록에서 제외하고 optional null/number로 검증하도록 수정했다.
- Security TODO와 MVP 릴리즈 체크리스트에 Realtime Database Rules 자동 테스트 항목을 반영했다.
- React 라우트 페이지를 `lazy`/`Suspense` 기반으로 분리해 홈, 채팅, 투표, 메모, 캘린더, 진단 화면을 필요 시점에 로드하도록 개선했다.
- Vite `manualChunks` 설정을 추가해 React, Firebase, Phosphor Icons vendor 코드를 별도 청크로 분리했다.
- 코드 스플리팅 후 커밋 정보가 동적 페이지 청크로 이동하는 구조에 맞춰 호스팅 스모크 스크립트가 참조된 JS 청크 전체에서 배포 커밋을 확인하도록 보완했다.

## 2026-09-12

- Firebase Hosting 이전 준비를 위해 `firebase.json`에 `dist` public directory와 SPA rewrite 설정을 추가했다.
- Dothome과 Firebase Hosting을 모두 지원하도록 Vite base path, React Router basename, 서비스 워커 등록, ErrorBoundary 홈 이동, 진단 화면 base path 검사를 Vite `BASE_URL` 기준으로 변경했다.
- PWA manifest와 service worker cache 경로를 상대/동적 경로로 변경해 `/our_share/` 하위 배포와 Firebase Hosting 루트 배포를 모두 지원하도록 개선했다.
- Firebase Hosting 루트 경로 빌드용 `npm run build:firebase` 스크립트를 추가했다.
- 호스팅 스모크 스크립트가 base URL 경로를 기준으로 asset 경로를 검사하도록 일반화했다.
- Dothome/Firebase Hosting 빌드 산출물의 asset 경로, PWA 파일, manifest/service worker 하드코딩 경로를 검증하는 `scripts/run-dist-smoke.mjs`를 추가했다.
- `npm run test:dist`, `npm run test:dist:firebase` 스크립트를 추가하고 Dothome 배포 workflow가 FTP 업로드 전 `dist` 산출물을 검사하도록 연결했다.
- GitHub Actions Dothome 배포 workflow가 Firebase Hosting 후보 빌드와 `test:dist:firebase`를 먼저 검증한 뒤 Dothome용 빌드를 다시 생성해 배포하도록 보완했다.
- MVP 최종 점검을 위해 lint, Firebase Rules 테스트, Dothome/Firebase Hosting 빌드와 산출물 검증을 순서대로 실행하는 `npm run test:mvp` 명령을 추가했다.
- 기본 배포 대상을 Dothome FTP에서 Firebase Hosting으로 전환하고 GitHub Actions workflow에서 Dothome FTP 업로드 단계를 제거했다.
- GitHub Actions가 `FIREBASE_SERVICE_ACCOUNT_OUR_SHARE_6BAF5` Secret이 있을 때 Firebase Hosting live 채널로 배포하고, Secret이 없으면 배포 단계만 건너뛰도록 구성했다.
- Firebase Hosting URL, Dothome legacy 상태, 필요한 GitHub Secret 정보를 배포 문서와 프로젝트 문서에 반영했다.

## 2026-09-14

- Firebase Hosting URL이 응답하지만 배포 JS에 최신 커밋 정보가 포함되지 않은 상태를 확인했다.
- GitHub Actions 배포 실패 원인을 빠르게 확인할 수 있도록 `FIREBASE_SERVICE_ACCOUNT_OUR_SHARE_6BAF5` Secret JSON 형식 검증 단계를 추가했다.
- Firebase 배포용 Secret은 계정 비밀번호가 아니라 Google Cloud 서비스 계정 키 JSON 전체 내용이어야 함을 배포 문서에 명확히 추가했다.
- GitHub Repository Secret 교체 후 빈 커밋으로 Firebase Hosting 자동배포를 재검증했다.
- GitHub Actions에서 Secret 검증, Firebase Hosting 배포, 공개 URL 스모크 테스트가 모두 성공함을 확인했다.
- `https://our-share-6baf5.web.app`에서 최신 커밋 `79549c6` 기준 주요 SPA 라우트가 정상 응답함을 확인했다.
- 채팅, 투표, 메모, 캘린더의 데스크톱 레이아웃을 좌측 액션 패널과 우측 리스트/내용 영역으로 통일했다.
- 모바일에서 투표, 메모, 캘린더, 채팅방 생성 폼을 기본 화면에서 분리하고 전체 화면 액션 레이어로 열리도록 정리했다.
- 투표 삭제, 메모 삭제, 채팅방 삭제 기능과 관련 Firestore 서비스 함수를 추가했다.
- 캘린더 기존 일정 삭제 흐름을 모바일 액션 레이어에서도 사용할 수 있도록 연결했다.
- 앱 헤더는 홈에서만 로고와 `우리 가족 스마트 홈` 문구를 표시하고, 나머지 화면에서는 하단 탭 이름과 같은 제목을 표시하도록 수정했다.
- 홈 화면의 강한 그라데이션 배경을 중립 카드와 브랜드 소프트 톤으로 단순화했다.
- 생성자 삭제 권한과 타 가족 구성원 삭제 차단을 검증하는 Firestore Rules 테스트를 추가했다.
- 헤더 우측 설정 버튼을 제거하고, 로그인 사용자 프로필 영역을 `rounded-full` 형태의 `내 정보` 진입 버튼으로 변경했다.
- `내 정보` 화면을 추가해 닉네임과 프로필 이미지 URL을 Firebase Auth 프로필과 Firestore `users` 문서에 저장할 수 있게 했다.
- `내 정보` 화면 헤더 우측에 설정 아이콘을 배치하고, `설정` 화면에는 박스 없는 뒤로가기 아이콘을 제공했다.
- 공통 `IconButton`에 박스 없는 `ghost` variant를 추가하고 모달/전체 화면 레이어 닫기 버튼에 적용했다.
- 캘린더 월간 날짜 셀의 일정 텍스트 표시를 점 마커로 바꾸고, 점 클릭 시 일정 상세 다이얼로그에서 내용 확인/수정/삭제가 가능하도록 변경했다.
- 캘린더 날짜 셀 클릭 시 해당 날짜가 시작일/종료일로 입력된 일정 등록 폼이 열리도록 연결했다.
- 모바일에서 등록 플로팅 버튼이 콘텐츠를 가리지 않도록 전역 하단 여백과 전체 화면 액션 레이어 하단 여백을 늘렸다.
- 모바일 입력 필드 포커스 시 확대되지 않도록 viewport 설정과 입력 요소 기본 글자 크기를 조정했다.
- 전역/앱 레이아웃에 가로 overflow 방지 설정을 추가하고 채팅/캘린더 그리드를 `minmax(0, ...)` 기반으로 보완했다.
- 캘린더 월간 날짜 셀을 `aspect-square` 기반 1:1 비율로 변경했다.
- 모바일 채팅에서 채팅방 목록을 먼저 보여주고, 방 클릭 시 `/chat/:roomId` 상세 화면으로 이동하며 헤더에 박스 없는 백버튼을 표시하도록 변경했다.
- 헤더에 알림 아이콘과 MVP 알림 리스트 드롭다운을 추가하고, 리스트 항목 클릭 시 캘린더/투표/채팅/메모 화면으로 이동하도록 연결했다.
- 첨부된 SVG를 PWA 앱 아이콘으로 교체하고, 설치된 PWA가 새 아이콘 캐시를 받을 수 있도록 서비스워커 캐시 버전을 `our-share-v2`로 올렸다.
- 채팅 페이지의 모바일 가로 overflow 후보를 보강하고, 채팅방 상세 화면에서는 `+ 채팅방` 플로팅 버튼이 표시되지 않도록 수정했다.
- 헤더를 fixed glass 스타일로 변경하고 1440 컨테이너 안에 콘텐츠만 정렬되도록 수정했다.
- 바텀 네비게이션을 세이프 영역을 고려한 glass 카드형으로 변경하고, 스크롤 중에는 메뉴 텍스트가 접히도록 처리했다.
- 채팅방 상세 같은 2뎁스 화면에서는 하단 네비게이션을 숨기도록 변경했다.
- 플로팅 등록 버튼을 glass 스타일로 바꾸고, 스크롤 중에는 아이콘만 보이도록 축약했다.
- Tailwind font-weight 토큰을 400/600 중심으로 정규화했다.
- 채팅방 상세 모바일 헤더에 채팅방 이름이 표시되도록 라우터 state를 연결했다.
- 채팅 상세 모바일 화면에서 카드형 박스를 제거하고, 메시지 영역이 디바이스 폭을 더 넓게 사용하도록 변경했다.
- 채팅 입력 영역 우측에 투표 아이콘 버튼과 전송 아이콘 버튼을 배치했다.
- 채팅 메시지를 프로필, 말풍선 꼬리, 내/상대방 배경 구분 형태로 바꾸고, 내 텍스트 메시지 수정/삭제 기능을 추가했다.
- 캘린더 등록 패널을 일정 등록/날짜 투표 탭으로 분리했다.
- 캘린더 날짜 셀의 일정 dot 터치 영역을 키우고 등록 건수를 함께 표시하도록 변경했다.
- PWA 홈 화면 설치 유도 바텀시트와 새 버전 업데이트/새로고침 안내 바텀시트를 추가했다.
- 오너 권한으로 가족 구성원을 삭제할 수 있는 서비스 함수와 홈 화면 액션을 추가했다.
- 채팅방 상세의 투표 버튼이 투표 1뎁스 페이지로 이동하지 않고, 채팅 위 전체 레이어에서 투표 생성 후 현재 채팅방으로 바로 전송되도록 변경했다.
- 홈화면 PWA 아이콘 공백 표시를 피하기 위해 첨부 SVG를 192/512 PNG 아이콘으로 렌더링하고 manifest와 apple-touch-icon에 우선 등록했다.
- 하단 네비게이션 메뉴 터치 시 이전 화면의 스크롤 위치를 유지하지 않고 최상단으로 이동하도록 변경했다.
- 페이지 전환 시 lazy chunk 로딩 fallback이 반복 노출되지 않도록 주요 페이지를 정적 import로 전환했다.
- 헤더 프로필 이미지/이니셜 영역의 테두리를 black 4% inside ring으로 변경했다.
- 채팅방 상세 화면의 메시지 입력 영역을 모바일 뷰포트 하단 fixed 입력바로 변경하고 메시지 영역 하단 여백을 보강했다.
- Firestore에 IndexedDB 기반 persistent local cache와 multi-tab manager를 적용해 재방문/탭 전환 시 데이터 로딩 체감을 줄이도록 변경했다.
- 채팅/메모/캘린더 화면에서 가족 정보 로딩 상태와 가족 없음 상태를 분리해 페이지 진입 직후 잘못된 빈 상태 안내가 보이지 않도록 개선했다.
- 채팅 메시지 수정/삭제와 오너의 가족 구성원 삭제 권한을 검증하는 Firestore Rules 회귀 테스트를 추가했다.
- MVP 진단 화면의 수동 QA 체크리스트를 최근 삭제/수정/PWA/모바일 2뎁스 흐름까지 포함하도록 보강했다.
- 앱 전역 Tailwind font weight 사용을 400/600 기준에 맞춰 `font-normal`/`font-semibold` 중심으로 정리했다.
- Post-MVP Admin SDK membership mirror 동기화 계획을 `PROJECT_CONTEXT.md`와 `TODO.md`에 구체화했다.
- 브라우저 기본 confirm/prompt를 제거하고 공통 Confirm Dialog 컴포넌트로 삭제 확인 UI를 일원화했다.
- 홈, 채팅, 투표, 메모, 캘린더, 내 정보의 주요 성공/오류 피드백을 공통 Toast 메시지로 연결했다.
- 채팅 메시지 수정 흐름을 브라우저 prompt 대신 앱 Modal 입력 UI로 변경했다.
- 투표 페이지의 데이터 로딩 함수를 안정화해 React Hooks lint 경고를 제거했다.
- `npm run test:mvp` 통합 점검에서 lint, Firebase Rules, Dothome/Firebase Hosting 산출물 검증이 모두 통과함을 확인했다.
- Firebase Hosting 이전 완료 상태에 맞춰 `DEPLOYMENT.md`의 남은 이전 작업 문구를 현재 자동배포 기준으로 정리했다.
- 브라우저 기본 alert/confirm/prompt 재도입을 막는 `npm run test:ui-feedback` 스모크 검사를 추가하고 MVP 통합 점검에 포함했다.
- 투표 메뉴와 채팅방 인라인 투표 생성 UI를 textarea 방식에서 항목별 추가/삭제 입력 방식으로 변경했다.
- 캘린더 날짜 투표 생성 UI를 직접 날짜 텍스트 입력 방식에서 월간 달력 터치 선택 방식으로 변경했다.
- 투표 항목 추가/삭제 입력 UI를 `PollOptionEditor` 공통 feature 컴포넌트로 분리해 투표 메뉴와 채팅방 인라인 투표에서 재사용하도록 정리했다.
- 날짜 투표 월간 선택 UI를 `DatePollPicker` 공통 feature 컴포넌트로 분리했다.
- 투표 메뉴와 채팅방 인라인 투표에서 날짜 타입 선택 시 `DatePollPicker`로 후보 날짜를 터치 선택하도록 일관성을 맞췄다.
- 투표 생성 버튼이 제목과 후보 항목/날짜 2개 이상이 준비된 경우에만 활성화되도록 개선하고 부족 조건 안내 문구를 추가했다.
- 캘린더 날짜 투표 생성 버튼도 제목과 후보 날짜 2개 이상이 준비된 경우에만 활성화되도록 맞췄다.
- 투표 생성 시 중복 후보를 저장하지 못하도록 서비스 검증을 추가하고, 일반 투표 생성 후 기본 후보 항목이 다시 채워지도록 정리했다.
- 날짜 투표 생성 시 복수 선택 옵션을 숨기고 저장 값도 단일 선택으로 고정해 일반 투표와 날짜 투표의 UI 의미를 분리했다.
- 일반 투표 후보 항목 중복을 생성 버튼 단계에서 미리 막고, 투표 메뉴와 채팅방 투표 만들기에서 같은 안내 문구를 보여주도록 공통 검증 유틸을 추가했다.
- 투표 후보 정규화/중복 검증 유틸의 유닛 테스트와 `npm run test:unit` 스크립트를 추가하고 MVP 통합 점검에 포함했다.
- 홈 화면 가족 위치 섹션에 Realtime Database 위치 좌표를 지도형 패널 위 가족 핀으로 표시하는 미니맵 UI를 추가했다.
- 홈 가족 위치 지도 핀을 CSS 기반 프로필 핀 형태로 변경하고, 지도 아래 위치 카드 목록은 핀 터치 시 바텀시트로 열리도록 축약했다.
- 홈 최근 채팅 요약의 긴 메시지가 카드 영역을 침범하지 않도록 한 줄 말줄임 처리를 보강했다.
- 홈 위치 지도/핀 UI 변경 사항이 설치된 PWA에 더 확실히 반영되도록 서비스워커 캐시 버전을 `our-share-v4`로 올렸다.
- 홈 화면의 가로 overflow를 줄이도록 카드/그리드 최소 폭을 보강하고, 가족 위치 핀을 원형 프로필과 하단 포인트 형태의 CSS 핀으로 다시 정리했다.
- 홈 레이아웃/지도 핀 수정 사항이 설치된 PWA에 반영되도록 서비스워커 캐시 버전을 `our-share-v5`로 올렸다.
- 홈 상단 구성원 요약을 2단 compact 카드로 바꾸고, 로그인 상태 패널에서 중복 프로필 정보를 제거해 전체 높이를 줄였다.
- 가족 위치 핀을 48px 단색 브랜드 컬러 핀으로 축소하고 Google 프로필 이미지가 원형 안을 꽉 채우도록 다시 정리했다.
- 홈 compact UI와 지도 핀 수정 사항이 설치된 PWA에 반영되도록 서비스워커 캐시 버전을 `our-share-v6`으로 올렸다.
- 홈 가족 구성원 섹션을 프로필, 이름, 역할, 모어 버튼 중심으로 간소화하고 역할 변경/삭제를 2뎁스 바텀시트 관리 흐름으로 분리했다.
- 홈 가족 구성원 관리 UI 변경 사항이 설치된 PWA에 반영되도록 서비스워커 캐시 버전을 `our-share-v7`로 올렸다.
- 구성원 역할 변경 바텀시트의 별도 뒤로가기 행을 제거하고, 백버튼을 타이틀 좌측 액션으로 배치했다.
- 구성원 역할 변경 타이틀 액션 변경 사항이 설치된 PWA에 반영되도록 서비스워커 캐시 버전을 `our-share-v8`로 올렸다.
- 홈 가족 관리 패널에서 중복 알림 켜기 버튼을 제거하고 위치 공유하기를 단독 주요 액션으로 배치했다.
- 홈 지도 위 위치 공유 인원/좌표 텍스트 박스를 제거해 지도 영역을 더 단순하게 정리했다.
- 홈 이달의 일정, 최근 메모와 투표, 최근 채팅 영역을 가로 스와이프 카드 섹션으로 묶고 스와이프 가능성을 보여주는 모션 인디케이터를 추가했다.
- 홈 스와이프 UI 변경 사항이 설치된 PWA에 반영되도록 서비스워커 캐시 버전을 `our-share-v9`로 올렸다.
- 홈 현재 가족 초대 코드 영역에 복사 아이콘 버튼을 추가하고, 복사 성공/실패를 Toast 메시지로 안내하도록 연결했다.
- 초대 코드 복사 UI 변경 사항이 설치된 PWA에 반영되도록 서비스워커 캐시 버전을 `our-share-v10`으로 올렸다.
- 홈 가족 위치 미니맵을 Kakao Map SDK 기반 실제 지도 배경으로 교체하고, 가족 위치 핀을 지도 좌표 위에 표시하도록 연결했다.
- Kakao Map JavaScript 키를 `VITE_KAKAO_MAP_JAVASCRIPT_KEY`로 교체 가능하게 `.env.example`에 추가하고 기본 키를 반영했다.
- Kakao Map 적용 사항이 설치된 PWA에 반영되도록 서비스워커 캐시 버전을 `our-share-v11`로 올렸다.
- Kakao Map 위 가족 위치 핀이 지도 타일 레이어에 가려지지 않도록 지도와 핀의 z-index 레이어를 명확히 분리했다.
- Kakao Map 핀 표시 수정 사항이 설치된 PWA에 반영되도록 서비스워커 캐시 버전을 `our-share-v12`로 올렸다.
- 홈 Kakao Map에서 사용자가 지도 확대/축소와 드래그를 할 수 있도록 활성화했다.
- 가족 위치 핀 프로필 이미지에 4px 브랜드 컬러 테두리를 적용하고 하단 포인트가 원형 프로필과 자연스럽게 이어지도록 조정했다.
- 지도 확대/축소 및 핀 디자인 변경 사항이 설치된 PWA에 반영되도록 서비스워커 캐시 버전을 `our-share-v13`으로 올렸다.
- 가족 위치 핀을 전달받은 `48x62` SVG path와 동일한 inline SVG 구조로 변경하고, 내부 Profile 원에 Google 프로필 이미지를 그대로 채우도록 수정했다.
- SVG 기반 지도 핀 변경 사항이 설치된 PWA에 반영되도록 서비스워커 캐시 버전을 `our-share-v14`로 올렸다.
- 홈 가족 위치 헤더 우측에 지도 초기화 버튼을 추가해 드래그/확대 후 가족 핀 bounds 기준 처음 상태로 돌아갈 수 있게 했다.
- 공통 바텀시트 닫기 버튼을 원형 박스 없이 아이콘만 보이는 ghost 스타일로 변경했다.
- 지도 초기화/바텀시트 닫기 UI 변경 사항이 설치된 PWA에 반영되도록 서비스워커 캐시 버전을 `our-share-v15`로 올렸다.
